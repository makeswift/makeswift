{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "format": "prettier --write .",
    "build": "turbo run build"
  },
  "devDependencies": {
    "@changesets/cli": "^2.22.0",
    "prettier": "^2.7.1",
    "turbo": "^1.4.3"
  }
}
