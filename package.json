{
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "pnpm": "^9.15.4"
  },
  "scripts": {
    "format": "prettier --write .",
    "build": "turbo run build --filter=!./apps/*",
    "test": "turbo run test",
    "madge": "turbo run madge",
    "check-builds": "turbo run build typecheck --filter=@makeswift/runtime --filter=@makeswift/next --filter=@makeswift/remix"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "@playwright/test": "^1.41.1",
    "@types/node": "^20.11.11",
    "prettier": "^3.2.5",
    "turbo": "^1.13.3"
  }
}
