import { useEffect } from 'react'
import { ActionTypes } from '../../../../state/actions'

export function useBuilderLocationSync({ appOrigin }: { appOrigin: string }) {
  useEffect(() => {
    // TODO
  }, [appOrigin])
}
