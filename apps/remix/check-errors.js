const puppeteer = require("puppeteer"); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); const errors = []; page.on("pageerror", error => { errors.push(error.message); }); page.on("console", msg => { if (msg.type() === "error") { console.log("Console error:", msg.text()); } }); await page.goto("http://localhost:4175/"); await new Promise(r => setTimeout(r, 2000)); console.log("Page errors:", errors); await browser.close(); })();
