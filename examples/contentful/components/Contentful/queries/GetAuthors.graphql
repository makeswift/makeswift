query GetAuthors($filter: AuthorFilter) {
  authorCollection(where: $filter) {
    items {
      __typename
      name
      avatar {
        __typename
        title
        description
        contentType
        fileName
        url
        width
        height
      }
      jobTitle
      description {
        json
      }
      slug
    }
  }
}
