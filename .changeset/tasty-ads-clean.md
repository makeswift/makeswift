---
"@makeswift/runtime": minor
---

Removes the DraftModeScript and PreviewModeScript from the runtime. These components are no longer needed for integrating a site with Makeswift, and can be safely removed from any existing code.
