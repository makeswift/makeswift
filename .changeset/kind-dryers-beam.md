---
"@makeswift/next-plugin": minor
"@makeswift/runtime": minor
---

BREAKING: replaces the runtime draft/preview mode proxy endpoint with middleware, and removes draft/preview rewrite rules from the next config decorator.
